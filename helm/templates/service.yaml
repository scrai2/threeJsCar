apiVersion: v1
kind: Service
metadata:
  name: {{ include "vyom-frontend-v2.fullname" . }}-service
  labels:
    {{- include "vyom-frontend-v2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vyom-frontend-v2.selectorLabels" . | nindent 4 }}
